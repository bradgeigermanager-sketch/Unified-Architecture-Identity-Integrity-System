Prompt: Cross-Domain Record Identification
Identify records across the specified domains and classify them based on shared attributes
(address, dates, identifiers). Highlight records requiring cross-domain reconciliation.

Domains:
{{domains}}

Collected Records:
{{collected_records}}

Prompt: Cross-Domain Alignment
Align records across domains and classify each record as aligned, misaligned, or conflicting.

Domains:
{{domains}}

Records:
{{records}}
