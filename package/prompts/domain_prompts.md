Prompt: Domain Record Extraction
Extract all records belonging to the specified domain. Classify each record as valid, invalid,
incomplete, or irrelevant based on domain schema.

Domain:
{{domain}}

Collected Records:
{{collected_records}}

Prompt: Domain Record Validation
Validate domain records against the domain schema. Identify records that are malformed,
incomplete, or inconsistent.

Domain:
{{domain}}

Records:
{{records}}
